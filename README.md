# rallie-landing

Landing page for Rallie.

GitHub Pages deploys `outputs/rallie-home` through `.github/workflows/deploy-pages.yml`.

Routes:

- `/` serves the landing page.
- `/app/` serves the court finder web app.

The court finder app talks to Supabase for shared public reports.
